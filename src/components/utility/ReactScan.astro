---
interface Props {
	enabled: boolean;
}

const { enabled } = Astro.props;
---

{enabled ? <script is:inline crossorigin="anonymous" src="//unpkg.com/react-scan/dist/auto.global.js"></script> : <></>}
